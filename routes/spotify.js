var express = require('express');
const SpotifyController = require("../controllers/SpotifyController");

var router = express.Router();

// router.get("/getdocuments", SpotifyController.docList);

module.exports = router;
